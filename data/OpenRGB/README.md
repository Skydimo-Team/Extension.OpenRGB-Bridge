# OpenRGB runtime (not in git)

The bridge launches `OpenRGB.exe` from this folder:

`ext.data_dir/OpenRGB/OpenRGB.exe`

Do not commit `.exe` / `.dll` / `.bin` here. Restore the Windows portable tree from the local backup:

`GitServer/github/_backup/Extension.OpenRGB-Bridge/data/OpenRGB/`

That copy is the exact bundle that used to live in this repo (OpenRGB.exe SHA256 `809AF78F6D628C61BEF50CD7BC0B1AEF9A327C757BBFBB7368FDA567E55A81E4`).
